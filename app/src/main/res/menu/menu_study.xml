<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.naohman.transsiberian.study.Study">
    <item android:id="@+id/empty"
        android:title="@string/action_settings"
        android:icon="@drawable/ic_action_overflow_light"
        android:orderInCategory="101"
        app:showAsAction="always">
        <menu>
            <item android:id="@+id/action_lang"
                android:title="@string/terms_first"
                android:checkable="true"
                android:checked="true"
                app:showAsAction="withText"
                android:orderInCategory="0"
                />
            <item android:id="@+id/action_restart"
                android:title="@string/restart"
                android:icon="@drawable/ic_action_refresh"
                app:showAsAction="always|withText"
                android:orderInCategory="1"
                />
            <item android:id="@+id/action_edit"
                android:title="@string/edit_set"
                android:icon="@drawable/ic_action_edit"
                app:showAsAction="always|withText"
                android:orderInCategory="2"
                />
        </menu>
    </item>
</menu>
